name: Upstream Sync (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain migrated flow
        shell: bash
        run: |
          set -Eeuo pipefail
          echo '上游合并已迁移到 AstrBot AI 合并流程。'
          echo '请通过 AstrBot 发起更新；本 workflow 不会修改 main、分支或 VPS。'
          exit 1
